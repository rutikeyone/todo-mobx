import 'package:flutter/material.dart';

class TextInputFieldTypeOne extends StatelessWidget {
  const TextInputFieldTypeOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}
